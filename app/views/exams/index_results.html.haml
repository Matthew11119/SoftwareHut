= link_to 'Back', results_url, class: "btn btn-default"

%h1= @exam.name

%h4
  = link_to "Download results CSV", exam_results_path(format: 'csv')
  %br
  = link_to "Download results PDFs", exam_pdfs_path(@exam)

%br
= render 'index_student_results'
= render 'index_stations'
