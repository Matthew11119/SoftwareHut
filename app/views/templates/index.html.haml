%h1 Listing templates

%table.table.table-hover
  %thead
    %tr
      %th Template Name
      %th
      %th
      %th

  %tbody
    - @templates.each do |template|
      %tr
        %td= template.exam_name
        %td= link_to 'Show', template
        %td= link_to 'Edit', edit_template_path(template)
        %td= link_to 'Destroy', template, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Template', new_template_path
